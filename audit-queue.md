# Audit queue (rotating)

# One entry per audit run. The first path line below is audited next; after each run it moves to the bottom of the list.
# Use repo-relative paths from the project root.
# Optional: multiple files in one audit — comma-separated (no spaces), e.g. csv-sorter/index.html,css/global.css
# Lines starting with # are ignored. Lines starting with - are ignored (markdown bullets).

csv-sorter/index.html
